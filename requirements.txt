# py-translate-book dependencies
# Requires Python >= 3.10
# Install:  pip install -r requirements.txt

# --- Core: EPUB read/write + HTML parsing ---
EbookLib>=0.18
beautifulsoup4>=4.12

# --- Translation engines ---
openai>=1.40
google-generativeai>=0.8,<0.9

# --- CLI progress + YAML config ---
tqdm>=4.66
PyYAML>=6.0

# --- Web UI (Flask pulls in Werkzeug + Jinja2 automatically) ---
Flask>=3,<4
